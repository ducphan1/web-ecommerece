// Xử lý sự kiện nút "Move All to Bag"
document.getElementById('moveToBag').addEventListener('click', function() {
    alert('All items moved to the bag!');
    // Logic để chuyển các sản phẩm từ wishlist vào bag có thể được thêm ở đây
});

// Xử lý sự kiện nút "See All"
document.getElementById('seeAll').addEventListener('click', function() {
    alert('See all products!');
    // Logic để hiển thị tất cả sản phẩm có thể được thêm ở đây
});
